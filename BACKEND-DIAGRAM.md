# Backend Architecture Diagram
**Visual Overview of Elvenwood Interiors Backend**

---

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                       CLIENT (Browser)                       │
│  Next.js 15 App Router (React + TypeScript)                 │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ HTTPS
                     │
┌────────────────────▼────────────────────────────────────────┐
│                    VERCEL EDGE NETWORK                       │
│  - CDN for static assets                                     │
│  - SSL/TLS termination                                       │
│  - DDoS protection                                           │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        │ Static                  │ API Routes
        │ Assets                  │ (Serverless)
        │                         │
        ▼                         ▼
┌───────────────┐      ┌─────────────────────┐
│  Next.js SSR  │      │  API Routes (13)    │
│  - Pages      │      │  ┌───────────────┐  │
│  - Components │      │  │ /api/projects │  │
│  - Layouts    │      │  │ /api/versions │  │
└───────────────┘      │  │ /api/invites  │  │
                       │  │ /api/upload   │  │
                       │  │ /api/auth     │  │
                       │  └───────┬───────┘  │
                       └──────────┼──────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
          ┌──────────────────┐      ┌──────────────────┐
          │ Supabase Client  │      │  Resend API      │
          │ (Connection Pool)│      │  (Email Service) │
          └────────┬─────────┘      └──────────────────┘
                   │
                   │ PostgreSQL Protocol
                   │ (Pooled via Supavisor)
                   │
          ┌────────▼─────────┐
          │   SUPABASE       │
          │  ┌────────────┐  │
          │  │ PostgreSQL │  │
          │  │   + RLS    │  │
          │  └────────────┘  │
          │  ┌────────────┐  │
          │  │   Storage  │  │
          │  │   (Files)  │  │
          │  └────────────┘  │
          │  ┌────────────┐  │
          │  │    Auth    │  │
          │  │   (JWT)    │  │
          │  └────────────┘  │
          └──────────────────┘
```

---

## 📊 Database Schema (Entity Relationship)

```
┌──────────────┐
│  auth.users  │ (Supabase Auth)
│  - id        │
│  - email     │
└──────┬───────┘
       │ 1:1
       │
┌──────▼───────┐         ┌──────────────┐
│  profiles    │         │  audit_logs  │
│  - id (PK)   │◄────────│  - user_id   │
│  - email     │         │  - action    │
│  - role      │         │  - changes   │
│  - full_name │         └──────────────┘
└──────┬───────┘
       │
       │ 1:N (as designer)
       │
┌──────▼───────┐
│  projects    │
│  - id (PK)   │◄────┐
│  - designer_id│    │
│  - client_id │    │
│  - name      │    │
│  - status    │    │
└──────┬───────┘    │
       │            │
       │ 1:N        │ 1:N
       │            │
┌──────▼───────┐  ┌─┴────────────┐
│  versions    │  │   invites    │
│  - id (PK)   │  │  - id (PK)   │
│  - project_id│  │  - project_id│
│  - version_# │  │  - token     │
│  - file_path │  │  - email     │
│  - changelog │  │  - status    │
└──────┬───────┘  └──────────────┘
       │
       │ 1:N
       │
┌──────▼───────┐
│  comments    │
│  - id (PK)   │
│  - version_id│
│  - author_id │
│  - content   │
│  - parent_id │◄─── (self-ref for threading)
└──────────────┘

┌──────────────┐
│notifications │
│  - id (PK)   │
│  - user_id   │
│  - type      │
│  - read      │
└──────────────┘
```

---

## 🔄 Request Flow: Create Project

```
 Designer                API Route             Database            Email
    │                       │                     │                  │
    │  1. POST /api/       │                     │                  │
    │     projects         │                     │                  │
    ├──────────────────────►                     │                  │
    │                       │  2. Verify JWT      │                  │
    │                       │     (Supabase Auth) │                  │
    │                       ├─────────────────────►                  │
    │                       │                     │                  │
    │                       │  3. Check profile   │                  │
    │                       ├─────────────────────►                  │
    │                       │◄─────────────────────                  │
    │                       │                     │                  │
    │                       │  4. INSERT project  │                  │
    │                       ├─────────────────────►                  │
    │                       │◄─────────────────────                  │
    │  5. Return project    │                     │                  │
    │◄──────────────────────┤                     │                  │
    │                       │                     │                  │
```

---

## 🔄 Request Flow: Invite Client

```
Designer              API Route            Database           Email Service
   │                     │                    │                    │
   │ 1. POST /api/      │                    │                    │
   │    send-invitation │                    │                    │
   ├────────────────────►                    │                    │
   │                     │ 2. Verify designer │                    │
   │                     ├────────────────────►                    │
   │                     │◄────────────────────                    │
   │                     │                    │                    │
   │                     │ 3. Generate token  │                    │
   │                     │    (UUID)          │                    │
   │                     │                    │                    │
   │                     │ 4. INSERT invite   │                    │
   │                     ├────────────────────►                    │
   │                     │◄────────────────────                    │
   │                     │                    │                    │
   │                     │ 5. Send email      │                    │
   │                     ├────────────────────────────────────────►
   │                     │                    │                    │
   │ 6. Return success   │                    │                    │
   │◄────────────────────┤                    │                    │
   │                     │                    │                    │

                                             │                    │
Client                                       │                    │
   │                                         │                    │
   │ 7. Receive email                        │                    │
   │◄────────────────────────────────────────────────────────────┤
   │                                         │                    │
   │ 8. Click link                           │                    │
   │    GET /accept-invite?token=xxx         │                    │
   ├─────────────────────►                   │                    │
   │                     │ 9. Validate token  │                    │
   │                     ├────────────────────►                    │
   │                     │◄────────────────────                    │
   │                     │                    │                    │
   │ 10. Show signup     │                    │                    │
   │     form            │                    │                    │
   │◄────────────────────┤                    │                    │
   │                     │                    │                    │
   │ 11. POST /api/      │                    │                    │
   │     accept-invite   │                    │                    │
   ├─────────────────────►                    │                    │
   │                     │ 12. Create user    │                    │
   │                     │     (Supabase Auth)│                    │
   │                     ├────────────────────►                    │
   │                     │◄────────────────────                    │
   │                     │                    │                    │
   │                     │ 13. Create profile │                    │
   │                     ├────────────────────►                    │
   │                     │                    │                    │
   │                     │ 14. Link to project│                    │
   │                     ├────────────────────►                    │
   │                     │                    │                    │
   │                     │ 15. Update invite  │                    │
   │                     ├────────────────────►                    │
   │                     │                    │                    │
   │ 16. Return session  │                    │                    │
   │◄────────────────────┤                    │                    │
   │                     │                    │                    │
```

---

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: HTTPS/TLS (Vercel Edge)                       │
│  - Encrypted transport                                   │
│  - SSL certificates                                      │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Layer 2: JWT Authentication (Supabase Auth)            │
│  - Token validation                                      │
│  - Session management                                    │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Layer 3: API Route Authorization                       │
│  - Role-based access control                            │
│  - Input validation                                      │
│  - Request sanitization                                  │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Layer 4: Row Level Security (PostgreSQL)               │
│  - Database-level permissions                           │
│  - Per-row access control                               │
│  - Automatic policy enforcement                         │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Layer 5: Audit Logging                                 │
│  - Track all actions                                     │
│  - Immutable logs                                        │
│  - Compliance & forensics                               │
└─────────────────────────────────────────────────────────┘
```

---

## ⚡ Performance Architecture

```
┌─────────────────────────────────────────────────────────┐
│  Vercel Edge Network (Global CDN)                       │
│  - 70+ edge locations                                    │
│  - Static asset caching                                  │
│  - Automatic compression                                 │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Serverless Functions (Auto-scaling)                    │
│  - Cold start: 200-500ms                                │
│  - Warm: 50-200ms                                       │
│  - Max concurrency: 30,000                              │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Connection Pooling (Supavisor)                         │
│  - 200 client connections                               │
│  - 20 backend connections (pooled)                      │
│  - Transaction mode                                      │
│  - 5x capacity increase                                 │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  Database Indexes                                        │
│  - B-tree indexes on primary keys                       │
│  - Composite indexes for common queries                 │
│  - 60-80% faster query execution                        │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│  PostgreSQL Database                                     │
│  - Optimized query planner                              │
│  - Connection pooling                                    │
│  - SSD storage                                           │
└─────────────────────────────────────────────────────────┘

Result: p95 = 341ms (⭐⭐⭐⭐⭐ Excellent)
```

---

## 📡 API Endpoints Map

```
/api/
│
├── projects/
│   ├── GET    /              → List all projects
│   ├── POST   /              → Create project
│   └── [id]/
│       ├── GET              → Get project details
│       ├── PATCH            → Update project
│       └── DELETE           → Delete project
│
├── versions/
│   ├── GET    /              → List versions
│   ├── POST   /              → Create version
│   └── [id]/
│       ├── GET              → Get version details
│       └── comments/
│           └── POST         → Add comment
│
├── send-invitation/
│   └── POST   /              → Send client invite
│
├── accept-invite/
│   └── POST   /              → Accept invitation
│
├── check-invite/
│   └── GET    /              → Validate token
│
├── upload/
│   └── POST   /              → Upload file
│
├── download/
│   └── GET    /              → Download file
│
├── notifications/
│   ├── GET    /              → List notifications
│   └── [id]/
│       └── PATCH            → Mark as read
│
└── auth/
    └── confirm-email/
        └── POST   /          → Confirm email

Total: 13 endpoints
```

---

## 🔄 Data Lifecycle

```
1. User Signs Up
   ├─► auth.users created (Supabase Auth)
   └─► profiles created (auto-trigger or on first login)

2. Designer Creates Project
   ├─► projects record created
   └─► designer_id linked

3. Designer Invites Client
   ├─► invites record created
   ├─► Unique token generated
   ├─► Email sent via Resend
   └─► Token expires in 7 days

4. Client Accepts Invite
   ├─► New user created (Supabase Auth)
   ├─► profiles record created (role: client)
   ├─► projects.client_id updated
   ├─► invites.status = 'accepted'
   └─► Redirect to project

5. Designer Uploads Version
   ├─► File uploaded to Supabase Storage
   ├─► versions record created
   ├─► version_number auto-incremented
   ├─► notifications created for client
   └─► Client receives notification

6. Client Adds Comment
   ├─► comments record created
   ├─► notifications created for designer
   └─► Designer receives notification

7. Project Completed
   ├─► projects.status = 'approved'
   ├─► Audit log created
   └─► Final notification sent
```

---

## 🎯 Performance Metrics Flow

```
Request
  │
  ├─► Vercel Edge Network (< 10ms)
  │
  ├─► Serverless Function Start (50-500ms)
  │   ├─► Cold start: 200-500ms
  │   └─► Warm: 50-200ms
  │
  ├─► Database Query (10-100ms)
  │   ├─► With indexes: 10-50ms  ✅
  │   └─► Without: 50-200ms  ⚠️
  │
  ├─► Connection Pool (< 5ms)
  │   ├─► Pooled: 1-5ms  ✅
  │   └─► Direct: 20-50ms  ⚠️
  │
  └─► Response (< 10ms)

Total: 100-500ms (Typical: 124ms avg, 341ms p95)
```

---

## 📊 Monitoring Dashboard (Conceptual)

```
┌─────────────────────────────────────────────────────────┐
│  GITHUB ACTIONS (Performance Testing)                   │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Weekly Load Test │  │ Deploy Check     │            │
│  │ Every Mon 2 AM   │  │ Every Push       │            │
│  │ Results: ✅      │  │ Results: ✅      │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  VERCEL DASHBOARD (Function Metrics)                    │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Invocations      │  │ Duration         │            │
│  │ 1,234/day        │  │ p95: 341ms       │            │
│  └──────────────────┘  └──────────────────┘            │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Error Rate       │  │ Bandwidth        │            │
│  │ 0%               │  │ 2 GB/month       │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  SUPABASE DASHBOARD (Database Metrics)                  │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Connections      │  │ Database Size    │            │
│  │ 45/200           │  │ 4.2 MB/500 MB    │            │
│  └──────────────────┘  └──────────────────┘            │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ Query Time       │  │ Storage Used     │            │
│  │ avg: 15ms        │  │ 0 MB/1 GB        │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Takeaways

### Architecture
✅ **Serverless** - Auto-scaling, pay-per-use
✅ **PostgreSQL** - Relational, ACID compliant
✅ **Edge Network** - Global CDN
✅ **JWT Auth** - Secure, stateless
✅ **Connection Pooling** - 5x capacity

### Performance
✅ **p95: 341ms** - Excellent response time
✅ **0% errors** - Highly reliable
✅ **30,000 concurrency** - Massive scale potential
✅ **60-80% faster** - With database indexes

### Security
✅ **5 layers** - Defense in depth
✅ **RLS** - Database-level security
✅ **Audit logs** - Complete traceability
✅ **Input validation** - SQL injection prevention

### Capacity
✅ **150-300 users** - Concurrent (optimized)
✅ **500-1000 projects** - Storage capacity
✅ **$0/month** - Free tier
✅ **99.9% uptime** - Vercel SLA

---

**See `BACKEND-ARCHITECTURE.md` for detailed documentation**
